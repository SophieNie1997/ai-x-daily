{
  "name": "ai-x-daily",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/generate-site.js ."
  }
}
